doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/application.css')
  body
    block content

    script.
      window.config = {};
      window.config.musicMap = {
        endpoint: '#{musicMapConfig.endpoint}',
        host: '#{musicMapConfig.host}',
      };
      window.config.fooBar = 'bar';
    script(src='/application.js')
